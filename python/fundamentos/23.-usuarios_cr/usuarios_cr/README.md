# Usuarios CR (Flask + MySQL)

1. Ejecuta `schema.sql` en MySQL Workbench.
2. Cambia la contraseña en `mysqlconnection.py`.
3. `pip install -r requirements.txt`
4. `python server.py` y abre http://127.0.0.1:5000/usuarios
